---
import Container from '@components/Container.astro';
import Navigation from '@components/Navigation.jsx';
---

<header class="w-full bg-white border-b z-50" transition:name='globalheader'>
    <Container>
        <Navigation client:only='preact'/>
    </Container>
</header>